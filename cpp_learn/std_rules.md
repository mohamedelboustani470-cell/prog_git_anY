#include<iostream>:mean that  mean input and output and calling some maim functions
std::;mean cout and cin are using just for prtnting and not something or code has the same name is like COUT (is for printf)
cout:is meaning that print that to the screen.
cin =get_some thing from the user,and scanf.
###escape sequence:
#\n: move the under line.
#\t:let space betwenn words.
#\a:remove space:
